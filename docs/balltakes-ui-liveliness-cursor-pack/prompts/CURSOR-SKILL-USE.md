For any multi-step Ball Takes frontend/UI task, use the project skill:

`balltakes-frontend-polish`

If Cursor does not automatically select it, invoke it manually from Agent using the skill picker/slash interface, then run the relevant phase prompt.

Use project Rules for persistent conventions and the Skill for the full audit/refactor/verify workflow.
