# Archived World Cup documents

These files are **not** current product spec. Ball Takes is a Premier League prediction product.

Kept here so historical prompts, bracket notes, and the edgy-reactions pack are not lost. Do not implement from this folder.

Live product sources:

- `docs/balltakes-product-remediation/`
- `frontend/src/reactions/` (reaction stickers in use)
- `WorldCupLegacyPurge` and `/brackets` → `/matchweek` remain as defense in depth
